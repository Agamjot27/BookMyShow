import { ProfilePage } from "@/components/profile/profile-page";

export const metadata = {
  title: "My Profile & Account Details | BookMyShow",
  description: "Manage your BookMyShow profile, view order history, saved devices, and preferences.",
};

export default function Page() {
  return <ProfilePage />;
}
