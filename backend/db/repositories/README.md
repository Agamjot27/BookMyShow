# Repositories

Place parameterized SQL data-access functions here as features are implemented.
Pass the checked-out pg client from db/transactions.ts for transactional work.
Do not call pool.query from inside a transaction.
